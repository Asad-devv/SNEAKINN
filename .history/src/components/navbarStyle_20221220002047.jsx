import {createTheme } from "@mui/material";

export const styles = createTheme({

    palette : {
        primary : {
            main : "#ffffff"
        }
    },
    typography : {
        fontFamily : "Unbounded",
        fontWeight: '900'
    }

})


